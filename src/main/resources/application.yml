spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springboot
    username: root
    password: monster@#12345
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create  # Automatically create/update tables
    show-sql: true  # Enable SQL query logging
